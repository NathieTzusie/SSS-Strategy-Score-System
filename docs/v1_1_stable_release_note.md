# v1.1 Stable Release Note

**版本：** Strategy Enable Score System v1.1.0  
**日期：** 2026-05-15  
**状态：** ✅ Stable — 通过验收

---

## 1. 版本结论

**v1.1 stable 已通过用户验收。**

- RC1 全部验证项 PASS
- 230/230 测试通过
- Baseline 稳定（delta=0, 0/3/3/6）
- 适合用于 **人工策略启停复盘**
- **不用于自动交易决策**

---

## 2. 当前可用能力

| 模块 | 能力 | 命令行 |
|------|------|--------|
| 默认评分 pipeline | 12 组合评分 + 性能矩阵 + Monte Carlo | `main --config config.yaml` |
| Label Quality | 修复 session/structure/snapshot | `label_quality --config config.yaml` |
| Partial Context Report | 6 字段环境上下文（只读） | `context_report --config config.yaml` |
| Data Quality Monitor | 系统健康 + Feature Gates + Baseline | `data_quality_monitor --config config.yaml` |
| CoinGlass Fetch | Live / mock / dry-run 抓取 | `coinglass_fetch --config ...` |
| Label Enrichment | 外部标签回填 | `label_enrichment --config ...` |

### 核心风险分析
- ✅ Monte Carlo 尾部回撤模拟
- ✅ TUW (水下时间比) / Recovery 风险
- ✅ Edge Concentration (收益集中度)
- ✅ Low Sample Warning
- ✅ Recent Health Penalty

---

## 3. 当前默认输入与输出

### 默认输入
```
config.yaml → input_path: outputs/data_quality/cleaned_trades.csv
```
- 516 笔交易，4 策略，3 regime
- **不使用 enriched_trades_full_year.csv 作为默认评分输入**

### 核心输出（每次运行 main 生成）

| 文件 | 说明 |
|------|------|
| `outputs/summary_report.md` | 评分汇总 + 风险分类（建议先看） |
| `outputs/enable_score.csv` | 12 组合 × 评分字段 |
| `outputs/performance_matrix.csv` | 14 项表现指标 |
| `outputs/monte_carlo_results.csv` | 10 项 MC 风险指标 |

### 只读上下文输出

| 文件 | 说明 |
|------|------|
| `outputs/context/partial_context_report.md` | OI/Funding/ETF/Session 分布 |
| `outputs/context/partial_context_summary.csv` | 结构化上下文数据 |

### 监控输出

| 文件 | 说明 |
|------|------|
| `outputs/monitor/data_quality_monitor_report.md` | 系统健康 + Feature Gates |
| `outputs/monitor/data_quality_monitor_summary.csv` | 结构化监控数据 |

---

## 4. 重要限制

| 限制 | 影响 | 缓解 |
|------|------|------|
| **orderflow_state 覆盖仅 14%** | Classifier + Market Opp 不可用 | 需 CoinGlass 升级或 Binance adapter |
| **Financial Calendar 不可用** | macro_state 无真实 event 标签 | 使用 ETF flow + 标注 fallback |
| **enriched CSV 非默认输入** | 评分不依赖外部标签 | Score regression delta=0, 暂不切换 |
| **Partial Context = INFORMATIONAL ONLY** | 不用于自动决策 | 标注清楚 |
| **不覆盖 live realtime monitoring** | 需手动运行 | 可用 VPS cron 定时运行 |

---

## 5. 使用建议

### 日常运行顺序

1. 每次新增交易数据后：
```bash
PYTHONPATH=src python3 -m strategy_enable_system.label_quality --config config.yaml
PYTHONPATH=src python3 -m strategy_enable_system.main --config config.yaml
PYTHONPATH=src python3 -m strategy_enable_system.data_quality_monitor --config config.yaml --output-dir outputs/monitor
```

2. 查看核心报告：
- `outputs/summary_report.md` — 评分
- `outputs/monitor/data_quality_monitor_report.md` — 系统健康

3. 可选生成 Context Report（辅助复盘）：
```bash
PYTHONPATH=src python3 -m strategy_enable_system.context_report --config config.yaml \
  --input outputs/data_quality/enriched_trades_full_year.csv \
  --quality-summary outputs/data_quality_full_year/label_quality_summary.csv \
  --output-dir outputs/context
```

### 不要做的

- ❌ 不要根据 Partial Context Report 直接开关策略
- ❌ 不要用不完整的 orderflow 数据做自动分类
- ❌ 不要在没有 shadow mode 验证的情况下接入 Market Opportunity Score

---

## 6. 下一阶段候选

| 序号 | 动作 | 说明 |
|------|------|------|
| 1 | 实际使用观察期 | 在复盘工作流中使用 v1.1，收集使用反馈 |
| 2 | Orderflow 数据源升级验证 | 确认 CoinGlass Startup 权限后验证 taker/CVD 12 月数据 |
| 3 | Binance adapter | 如 CoinGlass 不满足，开发免费 Binance adapter |
| 4 | Context Review 改进 | 基于使用反馈优化 context report 展示 |

---

*Generated by Strategy Enable Score System v1.1.0 — Stable Release*
